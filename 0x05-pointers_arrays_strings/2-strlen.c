#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - Return string length
 * @s: string length
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int a = strlen(s);

	return (a);
}

