#include "main.h"
#include <stdio.h>

/**
 * int _strlen - Return string length
 * @s: string length
 * Return: Always 0.
 */
int _strlen(char *s)
{
	size_t strlen(const char *string);
	int m;

	m = *s;
	*s = m;
}

